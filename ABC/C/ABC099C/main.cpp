#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
const int INF = INT32_MAX / 2;
int main(){
    int N;
    cin >> N;
    vector<int> dp(N + 1, INF);
    dp[0] = 0;
    for(int i=1; i<N; i++){
        
    }
}